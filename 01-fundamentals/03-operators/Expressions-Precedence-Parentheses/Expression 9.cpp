#include <iostream>
using namespace std;
int main()
{
  int a, b, y;
  cout << "Enter the value of a: ";
  cin >> a;
  cout << "Enter the value of b: ";
  cin >> b;
  y = (a + b) * (a + b);
  cout << "y= " << y << endl;
  return 0;
}